# Securecheck
Python security tool that scores password strength via entropy and scans URLs for phishing risk indicators.

## Features
- Password strength scoring using entropy calculation (based on character variety + length)
- URL phishing detection using regex pattern matching (IP-based hosts, redirect tricks, suspicious domains)
- Built with Python + Streamlit

## Run locally
